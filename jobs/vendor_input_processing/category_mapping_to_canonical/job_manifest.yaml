job_id: category_mapping_to_canonical
glue_job_name: TBD
runtime: pyspark
entrypoint: glue_script.py

parameters:
  - JOB_NAME
  - INPUT_BUCKET
  - OUTPUT_BUCKET
  - vendor_name
  - preprocessed_input_key
  - prepared_output_prefix

inputs:
  - bucket: ${INPUT_BUCKET}
    key_pattern: ${preprocessed_input_key}/${vendor_name}_vendor_products.json
    format: json
    required: true
  - bucket: ${INPUT_BUCKET}
    key_pattern: ${preprocessed_input_key}/${vendor_name}_product_category_links.json
    format: json
    required: true
  - bucket: ${INPUT_BUCKET}
    key_pattern: ${preprocessed_input_key}/${vendor_name}_vendor_categories.json
    format: json
    required: true
  - bucket: ${INPUT_BUCKET}
    key_pattern: canonical_mappings/Category_Mapping_Reference_*.json
    format: json
    required: false

outputs:
  - bucket: ${OUTPUT_BUCKET}
    key_pattern: ${prepared_output_prefix}/${vendor_name}_forMapping_products
    format: ndjson
    required: true

config_files: []

side_effects:
  deletes_inputs: false
  overwrites_outputs: true

logging_and_receipt:
  writes_run_receipt: false
  run_receipt_bucket: TBD
  run_receipt_key_pattern: TBD
  counters_observed: []

notes:
  - "Phase 1 reverse-documentation: derived only from TARGET_SCRIPT; unknowns are marked TBD."
  - "TBD_EXPLANATIONS: (auto-filled list below)"
  - "glue_job_name: TBD — script only consumes JOB_NAME argument; no fixed Glue job name is declared."
  - "logging_and_receipt.run_receipt_bucket: TBD — script does not write a run receipt artifact."
  - "logging_and_receipt.run_receipt_key_pattern: TBD — script does not write a run receipt artifact."
